<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.home.sort.SortActivity"></activity>
        <activity android:name=".ui.home.shouye.ShouActivity"></activity>
        <activity android:name=".ui.home.HotGoodsActivity" />
        <activity android:name=".ui.home.hom.BrandInfoActivity" />
        <activity android:name=".ui.home.banner.BannerHomeActivity" />
        <activity android:name=".test.HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.BaseA" />
        <activity android:name=".base.VHomeActivity" />
        <activity android:name=".ui.home.BindingHomeActivity" />
        <activity android:name=".tongpao.HotActivity"></activity>
    </application>

</manifest>